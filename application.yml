server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nthagipit/microservice-config
          clone-on-start: true
          default-label: master
